import{useState}from'react';import{useNavigate}from'react-router-dom';import styled from'styled-components';import{Leaf}from'lucide-react';import{api}from'../api';import{Button,Card,Page}from'../ui';
const Box=styled(Card)`width:min(100% - 32px,430px);margin:12vh auto;padding:40px;h1{font:500 36px Georgia,serif;color:#304a38}label{display:block;color:#657269;font-size:13px;margin:18px 0 7px}input{width:100%;padding:14px;border:1px solid #d2ddd2;border-radius:13px;background:white;outline:none;&:focus{border-color:#6f8f77}}button{width:100%;margin-top:24px}.error{color:#a05252}`;
export function Login(){const[email,setEmail]=useState('admin@example.ru'),[password,setPassword]=useState(''),[error,setError]=useState('');const nav=useNavigate();async function submit(e:React.FormEvent){e.preventDefault();try{const r=await api.post('/auth/login',{email,password});localStorage.setItem('admin_token',r.data.token);nav('/admin')}catch{setError('Не удалось войти. Проверьте данные.')}}return <Page><Box><Leaf color="#58735f"/><h1>Панель исследования</h1><form onSubmit={submit}><label>Электронная почта</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><label>Пароль</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/>{error&&<p className="error">{error}</p>}<Button>Войти</Button></form></Box></Page>}
